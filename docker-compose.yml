services:
  app:
    build: .
    container_name: otk-server
    restart: always
    ports:
      - "8083:8080"
    env_file:
      - .env
    volumes:
      - /root/OTKServer/src/main/resources/static/images:/app/images:ro
      - /root/OTKServer/documents:/app/documents:ro
